{
  "name": "my-mobile-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css script.js export.js import.js pdf.js pdfModal.js dist/"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
